<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Micropay — Every Message, One Transaction</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Clash+Display:wght@400;500;600;700&family=Bricolage+Grotesque:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #05050a;
  --surface: #0c0c14;
  --card: #111119;
  --card2: #16161f;
  --border: #1c1c28;
  --border2: #262636;
  --accent: #4f46e5;
  --accent2: #818cf8;
  --accent3: #c7d2fe;
  --green: #10b981;
  --usdc: #2775ca;
  --text: #f0f0f8;
  --muted: #64648a;
  --muted2: #9898b8;
  --radius: 16px;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html { scroll-behavior: smooth; }

body {
  font-family: 'Bricolage Grotesque', sans-serif;
  background: var(--bg);
  color: var(--text);
  min-height: 100vh;
  overflow-x: hidden;
}

/* ── Background ── */
.bg-fixed {
  position: fixed; inset: 0; z-index: 0; pointer-events: none;
  background:
    radial-gradient(ellipse 70% 50% at 10% 0%, #4f46e514 0%, transparent 55%),
    radial-gradient(ellipse 50% 60% at 90% 100%, #2775ca0e 0%, transparent 55%),
    radial-gradient(ellipse 40% 40% at 50% 50%, #4f46e508 0%, transparent 60%);
}
.bg-grid {
  position: fixed; inset: 0; z-index: 0; pointer-events: none;
  background-image:
    linear-gradient(var(--border) 1px, transparent 1px),
    linear-gradient(90deg, var(--border) 1px, transparent 1px);
  background-size: 40px 40px;
  opacity: 0.4;
  mask-image: radial-gradient(ellipse 100% 100% at 50% 0%, black 0%, transparent 80%);
}

/* ── Layout ── */
.page { position: relative; z-index: 1; }

/* ── NAV ── */
nav {
  position: fixed; top: 0; left: 0; right: 0; z-index: 100;
  display: flex; align-items: center; justify-content: space-between;
  padding: 0 32px; height: 60px;
  background: #05050acc;
  backdrop-filter: blur(16px);
  border-bottom: 1px solid var(--border);
}
.nav-logo { display: flex; align-items: center; gap: 10px; text-decoration: none; }
.nav-logo-mark {
  width: 30px; height: 30px;
  background: var(--accent);
  border-radius: 8px;
  display: flex; align-items: center; justify-content: center;
  font-size: 14px; font-weight: 700; color: #fff;
  font-family: 'Clash Display', sans-serif;
  letter-spacing: -1px;
}
.nav-logo-text {
  font-family: 'Clash Display', sans-serif;
  font-size: 17px; font-weight: 600;
  color: var(--text); letter-spacing: -0.3px;
}
.nav-right { display: flex; align-items: center; gap: 10px; }
.nav-link {
  font-size: 13px; color: var(--muted2); text-decoration: none;
  padding: 6px 12px; border-radius: 8px;
  transition: color 0.2s;
  display: none;
}
.nav-link:hover { color: var(--text); }
@media(min-width:768px){ .nav-link { display: block; } }
.nav-btn {
  font-size: 13px; font-weight: 500;
  background: var(--accent); color: #fff;
  border: none; border-radius: 9px;
  padding: 8px 18px; cursor: pointer;
  font-family: 'Bricolage Grotesque', sans-serif;
  transition: all 0.2s;
}
.nav-btn:hover { background: #4338ca; transform: translateY(-1px); }

/* ── HERO ── */
.hero {
  min-height: 100vh;
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  padding: 80px 24px 48px;
  text-align: center;
}
.hero-badge {
  display: inline-flex; align-items: center; gap: 7px;
  background: #4f46e510; border: 1px solid #4f46e530;
  border-radius: 100px; padding: 6px 16px;
  font-size: 12px; color: var(--accent2);
  margin-bottom: 28px;
  animation: fadeUp 0.6s both;
}
.hero-badge::before {
  content: ''; width: 6px; height: 6px;
  background: var(--green); border-radius: 50%;
  animation: pulse 2s infinite;
}
@keyframes pulse { 0%,100%{opacity:1} 50%{opacity:.3} }

.hero-title {
  font-family: 'Clash Display', sans-serif;
  font-size: clamp(36px, 7vw, 72px);
  font-weight: 700;
  line-height: 1.05;
  letter-spacing: -2px;
  margin-bottom: 24px;
  animation: fadeUp 0.6s 0.1s both;
}
.hero-title .accent { color: var(--accent2); }
.hero-title .line2 {
  display: block;
  background: linear-gradient(90deg, var(--accent2), #a5b4fc, var(--accent2));
  background-size: 200%;
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  animation: shimmer 4s linear infinite;
}
@keyframes shimmer { from{background-position:0%} to{background-position:200%} }

.hero-sub {
  font-size: clamp(14px, 2vw, 18px);
  color: var(--muted2); line-height: 1.7;
  max-width: 520px; margin: 0 auto 48px;
  animation: fadeUp 0.6s 0.2s both;
}

@keyframes fadeUp { from{opacity:0;transform:translateY(20px)} to{opacity:1;transform:translateY(0)} }

/* ── AUTH WIDGET ── */
.widget-wrap {
  width: 100%; max-width: 420px;
  animation: fadeUp 0.6s 0.3s both;
}
.widget {
  background: var(--card);
  border: 1px solid var(--border2);
  border-radius: 22px;
  overflow: hidden;
  box-shadow: 0 32px 80px #00000070, 0 0 0 1px #ffffff06 inset;
}
.widget-header {
  padding: 18px 24px 0;
  display: flex; align-items: center; gap: 10px;
}
.widget-dot { width: 8px; height: 8px; border-radius: 50%; }
.wd-red { background: #ff5f57; }
.wd-yellow { background: #febc2e; }
.wd-green { background: #28c840; }
.widget-title {
  margin-left: auto;
  font-size: 12px; color: var(--muted);
  font-family: 'Bricolage Grotesque', sans-serif;
}

/* Tabs */
.tab-row {
  display: flex; margin: 16px 16px 0;
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 11px; padding: 3px; gap: 3px;
}
.tab {
  flex: 1; padding: 9px; border-radius: 8px;
  border: none; font-family: 'Bricolage Grotesque', sans-serif;
  font-size: 13.5px; font-weight: 500;
  cursor: pointer; transition: all 0.2s;
  color: var(--muted); background: transparent;
}
.tab.active { background: var(--accent); color: #fff; box-shadow: 0 4px 12px #4f46e540; }

/* Form */
.form-body { padding: 20px 24px 24px; }
.form-title { font-family: 'Clash Display', sans-serif; font-size: 20px; font-weight: 600; margin-bottom: 4px; letter-spacing: -0.3px; }
.form-sub { font-size: 12.5px; color: var(--muted); margin-bottom: 20px; line-height: 1.5; }

.field { margin-bottom: 14px; }
.field label { display: block; font-size: 11px; font-weight: 600; color: var(--muted2); letter-spacing: 0.5px; margin-bottom: 6px; }
.input-wrap { position: relative; }
.input-icon { position: absolute; left: 13px; top: 50%; transform: translateY(-50%); font-size: 15px; color: var(--muted); pointer-events: none; }
.field input {
  width: 100%; background: var(--surface);
  border: 1px solid var(--border2); border-radius: 11px;
  padding: 11px 13px 11px 40px;
  color: var(--text); font-family: 'Bricolage Grotesque', sans-serif;
  font-size: 13.5px; outline: none;
  transition: border-color 0.2s, box-shadow 0.2s;
}
.field input::placeholder { color: var(--muted); }
.field input:focus { border-color: var(--accent); box-shadow: 0 0 0 3px #4f46e520; }
.eye { position: absolute; right: 12px; top: 50%; transform: translateY(-50%); background: none; border: none; color: var(--muted); cursor: pointer; font-size: 15px; padding: 0; transition: color 0.2s; }
.eye:hover { color: var(--muted2); }
.field-err { font-size: 11px; color: #f87171; margin-top: 4px; display: none; }
.field-err.show { display: block; }
.field-ok { font-size: 11px; color: var(--green); margin-top: 4px; }

.forgot { text-align: right; font-size: 12px; margin-top: -6px; margin-bottom: 16px; }
.forgot a { color: var(--accent2); text-decoration: none; }
.forgot a:hover { text-decoration: underline; }

.btn-main {
  width: 100%; padding: 13px;
  background: var(--accent);
  border: none; border-radius: 11px;
  color: #fff; font-family: 'Clash Display', sans-serif;
  font-size: 15px; font-weight: 600;
  cursor: pointer; transition: all 0.2s;
  box-shadow: 0 6px 20px #4f46e540;
  letter-spacing: -0.2px;
}
.btn-main:hover { background: #4338ca; transform: translateY(-1px); box-shadow: 0 8px 28px #4f46e550; }
.btn-main:active { transform: translateY(0); }
.btn-main:disabled { opacity: .5; cursor: not-allowed; transform: none; }

.divider { display: flex; align-items: center; gap: 10px; margin: 16px 0; color: var(--muted); font-size: 12px; }
.divider::before,.divider::after { content: ''; flex: 1; height: 1px; background: var(--border); }

.btn-google {
  width: 100%; padding: 12px;
  background: var(--surface); border: 1px solid var(--border2);
  border-radius: 11px; color: var(--text);
  font-family: 'Bricolage Grotesque', sans-serif;
  font-size: 13.5px; font-weight: 500;
  cursor: pointer; transition: all 0.2s;
  display: flex; align-items: center; justify-content: center; gap: 10px;
}
.btn-google:hover { background: var(--border); border-color: var(--accent); }

.switch { text-align: center; font-size: 12px; color: var(--muted); margin-top: 16px; }
.switch a { color: var(--accent2); text-decoration: none; cursor: pointer; font-weight: 500; }

/* Steps */
.steps-row { display: flex; gap: 5px; margin-bottom: 18px; }
.step { height: 3px; flex: 1; border-radius: 2px; background: var(--border2); transition: all .3s; }
.step.active { background: var(--accent); }
.step.done { background: var(--green); }

/* OTP */
.otp-grid { display: flex; gap: 6px; }
.otp-grid input { flex: 1; text-align: center; padding: 12px 0; font-size: 20px; font-weight: 700; border-radius: 10px; }

/* PW strength */
.pw-bars { display: flex; gap: 3px; margin-top: 6px; margin-bottom: 3px; }
.pw-bar { height: 3px; flex: 1; border-radius: 2px; background: var(--border2); transition: background .3s; }
.pw-note { font-size: 11px; color: var(--muted); }

/* Success */
.success-box { text-align: center; padding: 16px 0 8px; }
.success-icon { font-size: 48px; animation: bounceIn .5s both; margin-bottom: 12px; }
@keyframes bounceIn { 0%{transform:scale(.3);opacity:0} 60%{transform:scale(1.1)} 100%{transform:scale(1);opacity:1} }
.success-title { font-family: 'Clash Display', sans-serif; font-size: 20px; font-weight: 700; margin-bottom: 6px; }
.success-sub { font-size: 12.5px; color: var(--muted); line-height: 1.6; margin-bottom: 18px; }
.addr-preview { background: var(--surface); border: 1px solid var(--border2); border-radius: 10px; padding: 10px 14px; margin-bottom: 16px; text-align: left; }
.addr-preview .lbl { font-size: 10px; color: var(--muted); letter-spacing: .5px; margin-bottom: 3px; }
.addr-preview .val { font-size: 11px; font-family: monospace; color: var(--accent2); word-break: break-all; }

/* ── STATS STRIP ── */
.stats-strip {
  display: flex; justify-content: center; flex-wrap: wrap;
  gap: 0; margin: 0 auto 80px;
  max-width: 720px;
  border: 1px solid var(--border2);
  border-radius: 16px;
  overflow: hidden;
  background: var(--card);
  animation: fadeUp 0.6s 0.4s both;
}
.stat-item {
  flex: 1; min-width: 140px;
  padding: 20px 24px;
  border-right: 1px solid var(--border2);
  text-align: center;
}
.stat-item:last-child { border-right: none; }
.stat-num { font-family: 'Clash Display', sans-serif; font-size: 26px; font-weight: 700; color: var(--accent2); margin-bottom: 4px; }
.stat-label { font-size: 11.5px; color: var(--muted); }

/* ── HOW IT WORKS ── */
.section { padding: 80px 24px; max-width: 1100px; margin: 0 auto; }
.section-tag { font-size: 11.5px; color: var(--accent2); letter-spacing: 1px; font-weight: 600; margin-bottom: 12px; text-transform: uppercase; }
.section-title { font-family: 'Clash Display', sans-serif; font-size: clamp(28px, 4vw, 42px); font-weight: 700; letter-spacing: -1px; margin-bottom: 16px; }
.section-sub { font-size: 15px; color: var(--muted2); line-height: 1.7; max-width: 480px; margin-bottom: 48px; }

.steps-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 16px; }
.step-card {
  background: var(--card); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 24px;
  position: relative; overflow: hidden;
  transition: all 0.3s;
}
.step-card:hover { border-color: var(--border2); transform: translateY(-2px); box-shadow: 0 12px 32px #00000050; }
.step-card::before { content: ''; position: absolute; inset: 0; background: linear-gradient(135deg, #4f46e508, transparent); opacity: 0; transition: opacity .3s; }
.step-card:hover::before { opacity: 1; }
.step-num { font-family: 'Clash Display', sans-serif; font-size: 11px; font-weight: 700; color: var(--accent); letter-spacing: 1px; margin-bottom: 14px; }
.step-icon { font-size: 28px; margin-bottom: 12px; }
.step-name { font-family: 'Clash Display', sans-serif; font-size: 16px; font-weight: 600; margin-bottom: 8px; letter-spacing: -0.2px; }
.step-desc { font-size: 13px; color: var(--muted2); line-height: 1.6; }

/* ── FEATURES ── */
.features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 16px; margin-top: 16px; }
.feature-card {
  background: var(--card); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 28px;
  transition: all 0.3s;
}
.feature-card:hover { border-color: var(--border2); transform: translateY(-2px); }
.feature-card.highlight { border-color: #4f46e530; background: linear-gradient(135deg, #4f46e508, var(--card)); }
.feature-icon-wrap { width: 44px; height: 44px; border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 20px; margin-bottom: 16px; }
.fi-purple { background: #4f46e520; }
.fi-blue { background: #2775ca20; }
.fi-green { background: #10b98120; }
.fi-orange { background: #f5973220; }
.feature-name { font-family: 'Clash Display', sans-serif; font-size: 17px; font-weight: 600; margin-bottom: 8px; letter-spacing: -0.2px; }
.feature-desc { font-size: 13px; color: var(--muted2); line-height: 1.65; }

/* ── TX DEMO ── */
.tx-demo {
  background: var(--card); border: 1px solid var(--border2);
  border-radius: 20px; padding: 24px;
  max-width: 560px;
}
.tx-demo-title { font-size: 12px; color: var(--muted); letter-spacing: .5px; margin-bottom: 16px; font-weight: 600; }
.tx-row {
  display: flex; align-items: center; gap: 12px;
  padding: 11px 14px;
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 11px; margin-bottom: 8px;
  animation: txSlide .4s both;
}
@keyframes txSlide { from{opacity:0;transform:translateX(-10px)} to{opacity:1;transform:translateX(0)} }
.tx-row:nth-child(2) { animation-delay: .1s; }
.tx-row:nth-child(3) { animation-delay: .2s; }
.tx-row:nth-child(4) { animation-delay: .3s; }
.msg-bubble { background: var(--accent); color: #fff; border-radius: 8px; padding: 4px 10px; font-size: 12px; flex-shrink: 0; }
.tx-arrow { color: var(--muted); font-size: 12px; flex-shrink: 0; }
.tx-badge { display: flex; align-items: center; gap: 5px; background: #10b98115; border: 1px solid #10b98130; border-radius: 6px; padding: 3px 8px; font-size: 11px; color: var(--green); flex-shrink: 0; margin-left: auto; white-space: nowrap; }
.tx-hash { font-size: 10.5px; font-family: monospace; color: var(--muted); flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

/* ── FOOTER ── */
footer {
  border-top: 1px solid var(--border);
  padding: 32px 24px;
  text-align: center;
  color: var(--muted); font-size: 12.5px;
}
footer .footer-logo { font-family: 'Clash Display', sans-serif; font-size: 16px; font-weight: 600; color: var(--text); margin-bottom: 8px; }
footer a { color: var(--muted2); text-decoration: none; }
footer a:hover { color: var(--text); }

/* ── TOAST ── */
.toast {
  position: fixed; bottom: 24px; left: 50%; transform: translateX(-50%);
  background: var(--card2); border: 1px solid var(--border2);
  border-radius: 12px; padding: 10px 18px;
  font-size: 13px; display: flex; align-items: center; gap: 8px;
  box-shadow: 0 12px 32px #00000070;
  opacity: 0; transition: opacity .3s; z-index: 999;
  pointer-events: none; white-space: nowrap;
}
.toast.show { opacity: 1; }

/* ── SPINNER ── */
.spin { display: inline-block; width: 14px; height: 14px; border: 2px solid #fff4; border-top-color: #fff; border-radius: 50%; animation: spin .6s linear infinite; vertical-align: middle; margin-right: 6px; }
@keyframes spin { to{transform:rotate(360deg)} }

/* ── MOBILE ── */
@media (max-width: 640px) {
  nav { padding: 0 16px; }
  .hero { padding: 80px 16px 40px; }
  .widget-wrap { max-width: 100%; }
  .stats-strip { border-radius: 12px; }
  .stat-item { min-width: 120px; padding: 16px; }
  .section { padding: 60px 16px; }
  .steps-grid { grid-template-columns: 1fr 1fr; }
  .features-grid { grid-template-columns: 1fr; }
  footer { padding: 24px 16px; }
}
@media (max-width: 400px) {
  .steps-grid { grid-template-columns: 1fr; }
}
</style>
</head>
<body>

<div class="bg-fixed"></div>
<div class="bg-grid"></div>

<div class="page">

<!-- ── NAV ── -->
<nav>
  <a href="#" class="nav-logo">
    <div class="nav-logo-mark">M</div>
    <div class="nav-logo-text">Micropay</div>
  </a>
  <div class="nav-right">
    <a href="#how" class="nav-link">How it works</a>
    <a href="#features" class="nav-link">Features</a>
    <button class="nav-btn" onclick="scrollToWidget()">Get Started</button>
  </div>
</nav>

<!-- ── HERO ── -->
<section class="hero">
  <div class="hero-badge">Arc Blockchain · Powered by Circle · Every Message = One Transaction</div>

  <h1 class="hero-title">
    AI Chat with<br>
    <span class="line2">Real Onchain Payments</span>
  </h1>

  <p class="hero-sub">
    Sign once. Pay per message. Every AI interaction becomes a verified blockchain transaction — seamless as ChatGPT, transparent as crypto.
  </p>

  <!-- AUTH WIDGET -->
  <div class="widget-wrap" id="authWidget">
    <div class="widget">
      <div class="widget-header">
        <div class="widget-dot wd-red"></div>
        <div class="widget-dot wd-yellow"></div>
        <div class="widget-dot wd-green"></div>
        <div class="widget-title">micropay.xyz</div>
      </div>

      <div class="tab-row">
        <button class="tab active" id="tabIn" onclick="switchTab('in')">Sign In</button>
        <button class="tab" id="tabUp" onclick="switchTab('up')">Sign Up</button>
      </div>

      <!-- SIGN IN -->
      <div id="formIn" class="form-body">
        <div class="form-title">Welcome back</div>
        <div class="form-sub">Sign in to your Micropay wallet</div>

        <div class="field">
          <label>EMAIL</label>
          <div class="input-wrap">
            <span class="input-icon">✉️</span>
            <input type="email" id="inEmail" placeholder="you@email.com" oninput="clrErr('inEmailErr')">
          </div>
          <div class="field-err" id="inEmailErr">Invalid email address</div>
        </div>

        <div class="field">
          <label>PASSWORD</label>
          <div class="input-wrap">
            <span class="input-icon">🔑</span>
            <input type="password" id="inPass" placeholder="••••••••" oninput="clrErr('inPassErr')">
            <button class="eye" onclick="toggleEye('inPass',this)">👁</button>
          </div>
          <div class="field-err" id="inPassErr">Incorrect password</div>
        </div>

        <div class="forgot"><a href="#">Forgot password?</a></div>

        <button class="btn-main" id="inBtn" onclick="doSignIn()">Sign In</button>

        <div class="divider">or</div>

        <button class="btn-google" onclick="doGoogle()">
          <svg width="17" height="17" viewBox="0 0 24 24"><path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/><path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/><path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z"/><path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/></svg>
          Continue with Google
        </button>

        <div class="switch">No account? <a onclick="switchTab('up')">Sign up free</a></div>
      </div>

      <!-- SIGN UP -->
      <div id="formUp" class="form-body" style="display:none">
        <!-- Step 1 -->
        <div id="up1">
          <div class="steps-row"><div class="step active"></div><div class="step"></div><div class="step"></div></div>
          <div class="form-title">Create account</div>
          <div class="form-sub">Step 1 / 3 — Your details</div>

          <div class="field">
            <label>USERNAME</label>
            <div class="input-wrap">
              <span class="input-icon" style="font-size:13px;font-weight:700;color:var(--muted2)">@</span>
              <input type="text" id="upUser" placeholder="your_name" oninput="checkUser(this)">
            </div>
            <div class="field-err" id="upUserErr"></div>
            <div class="field-ok" id="upUserOk" style="display:none"></div>
          </div>

          <div class="field">
            <label>EMAIL</label>
            <div class="input-wrap">
              <span class="input-icon">✉️</span>
              <input type="email" id="upEmail" placeholder="you@email.com" oninput="clrErr('upEmailErr')">
            </div>
            <div class="field-err" id="upEmailErr">Invalid email address</div>
          </div>

          <div class="field">
            <label>PASSWORD</label>
            <div class="input-wrap">
              <span class="input-icon">🔑</span>
              <input type="password" id="upPass" placeholder="At least 8 characters" oninput="checkPW(this)">
              <button class="eye" onclick="toggleEye('upPass',this)">👁</button>
            </div>
            <div class="pw-bars"><div class="pw-bar" id="b1"></div><div class="pw-bar" id="b2"></div><div class="pw-bar" id="b3"></div><div class="pw-bar" id="b4"></div></div>
            <div class="pw-note" id="pwNote">Enter a password</div>
          </div>

          <button class="btn-main" onclick="upNext1()">Continue →</button>
          <div class="switch" style="margin-top:14px">Have an account? <a onclick="switchTab('in')">Sign in</a></div>
        </div>

        <!-- Step 2: OTP -->
        <div id="up2" style="display:none">
          <div class="steps-row"><div class="step done"></div><div class="step active"></div><div class="step"></div></div>
          <div class="form-title">Check your email 📬</div>
          <div class="form-sub">Code sent to <b id="upEmailShow"></b></div>
          <div class="field">
            <label>6-DIGIT CODE</label>
            <div class="otp-grid" id="otpGrid">
              <input type="text" maxlength="1" oninput="otpFwd(this,0)" onkeydown="otpBck(event,0)">
              <input type="text" maxlength="1" oninput="otpFwd(this,1)" onkeydown="otpBck(event,1)">
              <input type="text" maxlength="1" oninput="otpFwd(this,2)" onkeydown="otpBck(event,2)">
              <input type="text" maxlength="1" oninput="otpFwd(this,3)" onkeydown="otpBck(event,3)">
              <input type="text" maxlength="1" oninput="otpFwd(this,4)" onkeydown="otpBck(event,4)">
              <input type="text" maxlength="1" oninput="otpFwd(this,5)" onkeydown="otpBck(event,5)">
            </div>
          </div>
          <button class="btn-main" id="otpBtn" onclick="verifyOTP()">Verify</button>
          <div class="switch" style="margin-top:12px">No code? <a id="resendA" onclick="resend()">Resend</a><span id="resendT" style="display:none;color:var(--muted)"> (<span id="tNum">60</span>s)</span></div>
        </div>

        <!-- Step 3: Done -->
        <div id="up3" style="display:none">
          <div class="steps-row"><div class="step done"></div><div class="step done"></div><div class="step active"></div></div>
          <div class="success-box">
            <div class="success-icon">🎉</div>
            <div class="success-title">Wallet created!</div>
            <div class="success-sub">Your Micropay wallet is live on Arc Testnet. No seed phrase needed.</div>
            <div class="addr-preview">
              <div class="lbl">WALLET ADDRESS</div>
              <div class="val" id="walletAddr"></div>
            </div>
            <button class="btn-main" onclick="goDashboard()">Open Dashboard →</button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Stats -->
  <div class="stats-strip" style="margin-top:48px">
    <div class="stat-item"><div class="stat-num">~$0.001</div><div class="stat-label">Gas per message</div></div>
    <div class="stat-item"><div class="stat-num">&lt;1s</div><div class="stat-label">Transaction speed</div></div>
    <div class="stat-item"><div class="stat-num">1×</div><div class="stat-label">Sign required</div></div>
    <div class="stat-item"><div class="stat-num">100%</div><div class="stat-label">Onchain verified</div></div>
  </div>
</section>

<!-- ── HOW IT WORKS ── -->
<section class="section" id="how">
  <div class="section-tag">How it works</div>
  <div class="section-title">Four steps to<br>autonomous AI payments</div>
  <div class="section-sub">Connect once, sign once — then every message flows automatically as a real blockchain transaction.</div>

  <div class="steps-grid">
    <div class="step-card">
      <div class="step-num">STEP 01</div>
      <div class="step-icon">🔗</div>
      <div class="step-name">Connect Wallet</div>
      <div class="step-desc">Sign in with Google or connect your existing wallet. A smart wallet is created automatically — no seed phrase.</div>
    </div>
    <div class="step-card">
      <div class="step-num">STEP 02</div>
      <div class="step-icon">🔑</div>
      <div class="step-name">Sign Session Key</div>
      <div class="step-desc">Authorize the agent once. Set a spending limit — e.g. max 10 USDC per 24 hours. Sign once, done forever.</div>
    </div>
    <div class="step-card">
      <div class="step-num">STEP 03</div>
      <div class="step-icon">💬</div>
      <div class="step-name">Send Messages</div>
      <div class="step-desc">Chat naturally. Each message triggers a real micropayment onchain — no popups, no confirmations needed.</div>
    </div>
    <div class="step-card">
      <div class="step-num">STEP 04</div>
      <div class="step-icon">⛓</div>
      <div class="step-name">Every TX Recorded</div>
      <div class="step-desc">Message #1 → TX #1. Message #2 → TX #2. Every AI interaction is a verifiable economic activity onchain.</div>
    </div>
  </div>

  <!-- TX Demo visual -->
  <div style="margin-top:40px">
    <div class="tx-demo">
      <div class="tx-demo-title">LIVE TRANSACTION FEED</div>
      <div class="tx-row">
        <div class="msg-bubble">MSG #1</div>
        <div class="tx-arrow">→</div>
        <div class="tx-hash">0x7f3a…e2f3</div>
        <div class="tx-badge">✓ 0.01 USDC</div>
      </div>
      <div class="tx-row">
        <div class="msg-bubble">MSG #2</div>
        <div class="tx-arrow">→</div>
        <div class="tx-hash">0x4b2c…a1d8</div>
        <div class="tx-badge">✓ 0.01 USDC</div>
      </div>
      <div class="tx-row">
        <div class="msg-bubble">MSG #3</div>
        <div class="tx-arrow">→</div>
        <div class="tx-hash">0x9e1f…c4b7</div>
        <div class="tx-badge">✓ 0.01 USDC</div>
      </div>
    </div>
  </div>
</section>

<!-- ── FEATURES ── -->
<section class="section" id="features">
  <div class="section-tag">Core features</div>
  <div class="section-title">Built for the<br>agent economy</div>
  <div class="section-sub">Every design decision optimized for seamless AI micropayments at scale.</div>

  <div class="features-grid">
    <div class="feature-card highlight">
      <div class="feature-icon-wrap fi-purple">🔑</div>
      <div class="feature-name">Session Key</div>
      <div class="feature-desc">Sign once, authorize a spending limit, and let the agent execute payments autonomously. No more popup fatigue. Built on ERC-4337 Account Abstraction.</div>
    </div>
    <div class="feature-card">
      <div class="feature-icon-wrap fi-blue">💵</div>
      <div class="feature-name">USDC Gas Fees</div>
      <div class="feature-desc">Pay gas in USDC — stable and predictable. No volatile native token needed. Costs ~$0.001 per transaction on Arc.</div>
    </div>
    <div class="feature-card">
      <div class="feature-icon-wrap fi-green">⚡</div>
      <div class="feature-name">Sub-second Finality</div>
      <div class="feature-desc">Arc's high-performance L1 settles transactions in under one second. Your AI responds before you notice the payment.</div>
    </div>
    <div class="feature-card">
      <div class="feature-icon-wrap fi-orange">📊</div>
      <div class="feature-name">Full Transparency</div>
      <div class="feature-desc">Every message, every payment — visible on Arcscan. Complete audit trail of every AI economic interaction you've made.</div>
    </div>
  </div>
</section>

<!-- ── FOOTER ── -->
<footer>
  <div class="footer-logo">Micropay</div>
  <p style="margin-bottom:12px">Every Message = One Transaction · Arc Testnet · Powered by Circle</p>
  <p><a href="#">Privacy</a> · <a href="#">Terms</a> · <a href="#">Docs</a> · <a href="#">GitHub</a></p>
  <p style="margin-top:16px;font-size:11px">© 2026 Micropay · v0.1.0-beta</p>
</footer>

</div><!-- .page -->

<div class="toast" id="toast"></div>

<script>
// ── Helpers ──────────────────────────────────
function $(id){ return document.getElementById(id); }
function showToast(msg, icon='✓'){
  const t = $('toast');
  t.innerHTML = `<span>${icon}</span> ${msg}`;
  t.classList.add('show');
  setTimeout(()=>t.classList.remove('show'), 2500);
}
function clrErr(id){ $(id).classList.remove('show'); }
function showErr(id, msg){ const e=$(id); if(msg)e.textContent=msg; e.classList.add('show'); }
function toggleEye(id, btn){
  const el=$(id);
  el.type = el.type==='password'?'text':'password';
  btn.textContent = el.type==='password'?'👁':'🙈';
}
function setLoad(id, on, label=''){
  const b=$(id); b.disabled=on;
  b.innerHTML = on ? '<span class="spin"></span>Processing…' : label;
}
function scrollToWidget(){ $('authWidget').scrollIntoView({behavior:'smooth', block:'center'}); }

// ── Tab switch ───────────────────────────────
function switchTab(t){
  $('formIn').style.display = t==='in'?'block':'none';
  $('formUp').style.display = t==='up'?'block':'none';
  $('tabIn').classList.toggle('active', t==='in');
  $('tabUp').classList.toggle('active', t==='up');
}

// ── Sign In ──────────────────────────────────
function doSignIn(){
  const e=$('inEmail').value.trim();
  const p=$('inPass').value;
  let ok=true;
  if(!e||!e.includes('@')){ showErr('inEmailErr'); ok=false; }
  if(!p||p.length<6){ showErr('inPassErr','Password min 6 characters'); ok=false; }
  if(!ok) return;
  setLoad('inBtn',true);
  setTimeout(()=>{ setLoad('inBtn',false,'Sign In'); window.location.href='dashboard.html'; }, 1200);
}
function doGoogle(){
  setLoad('inBtn',true);
  setTimeout(()=>{ setLoad('inBtn',false,'Sign In'); window.location.href='dashboard.html'; }, 900);
}

// ── Sign Up ──────────────────────────────────
const reserved = ['admin','micropay','arc','usdc','circle','system'];
function checkUser(el){
  const v = el.value.toLowerCase().trim();
  $('upUserErr').classList.remove('show');
  $('upUserOk').style.display='none';
  if(!v) return;
  if(v.length<3){ showErr('upUserErr','Min 3 characters'); return; }
  if(!/^[a-z0-9_]+$/.test(v)){ showErr('upUserErr','Lowercase letters, numbers, _ only'); return; }
  if(reserved.includes(v)){ showErr('upUserErr','Username taken'); return; }
  $('upUserOk').textContent = `✓ @${v} is available`;
  $('upUserOk').style.display='block';
}
function checkPW(el){
  const v=el.value; let s=0;
  if(v.length>=8)s++; if(/[A-Z]/.test(v))s++; if(/[0-9]/.test(v))s++; if(/[^A-Za-z0-9]/.test(v))s++;
  const colors=['','#ef4444','#f59e0b','#3b82f6','#10b981'];
  const labels=['Enter a password','Weak','Fair','Strong','Very strong'];
  ['b1','b2','b3','b4'].forEach((id,i)=>{ $(id).style.background = i<s ? colors[s] : 'var(--border2)'; });
  $('pwNote').textContent = labels[s];
  $('pwNote').style.color = colors[s]||'var(--muted)';
}
function upNext1(){
  const u=$('upUser').value.trim();
  const e=$('upEmail').value.trim();
  const p=$('upPass').value;
  if(!u||u.length<3){ showErr('upUserErr','Min 3 characters'); return; }
  if(!e||!e.includes('@')){ showErr('upEmailErr'); return; }
  if(!p||p.length<8){ showToast('Password must be 8+ characters','⚠️'); return; }
  $('up1').style.display='none';
  $('up2').style.display='block';
  $('upEmailShow').textContent=e;
  startTimer();
  showToast('Verification code sent to '+e,'✉️');
  setTimeout(()=>$('otpGrid').querySelectorAll('input')[0].focus(),100);
}
function otpFwd(el,i){
  el.value=el.value.replace(/\D/g,'');
  const ins=$('otpGrid').querySelectorAll('input');
  if(el.value&&i<5) ins[i+1].focus();
  const code=Array.from(ins).map(x=>x.value).join('');
  if(code.length===6) verifyOTP();
}
function otpBck(e,i){
  if(e.key==='Backspace'){
    const ins=$('otpGrid').querySelectorAll('input');
    if(!ins[i].value&&i>0){ ins[i-1].focus(); ins[i-1].value=''; }
  }
}
function verifyOTP(){
  const code=Array.from($('otpGrid').querySelectorAll('input')).map(x=>x.value).join('');
  if(code.length<6){ showToast('Enter all 6 digits','⚠️'); return; }
  setLoad('otpBtn',true);
  setTimeout(()=>{
    setLoad('otpBtn',false,'Verify');
    $('up2').style.display='none';
    $('up3').style.display='block';
    $('walletAddr').textContent='0x'+Array.from({length:40},()=>'0123456789abcdef'[~~(Math.random()*16)]).join('');
  }, 1000);
}
let timerI;
function startTimer(){
  let t=60;
  $('resendA').style.display='none'; $('resendT').style.display='inline';
  clearInterval(timerI);
  timerI=setInterval(()=>{ $('tNum').textContent=--t; if(t<=0){ clearInterval(timerI); $('resendA').style.display='inline'; $('resendT').style.display='none'; } },1000);
}
function resend(){ showToast('Code resent!','✉️'); startTimer(); }
function goDashboard(){ window.location.href='dashboard.html'; }

// ── Scroll nav highlight ─────────────────────
window.addEventListener('scroll', ()=>{
  document.querySelector('nav').style.background = window.scrollY > 10 ? '#05050af0' : '#05050acc';
});
</script>
</body>
</html>
